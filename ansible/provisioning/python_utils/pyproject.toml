[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "serverlessyarn_utils"
version = "0.1.0"

[tool.hatch.build.targets.wheel]
packages = ["src/serverlessyarn_utils"]