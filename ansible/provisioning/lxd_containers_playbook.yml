# Setup nodes
- hosts: nodes
  become: yes

  vars_files:
    - vars/main.yml
    
  handlers:
    - import_tasks: handlers/main.yml
  tasks:
    - import_tasks: tasks/lxd_setup/lxd_containers_setup.yml
