- name: Tmux session check
  shell: tmux ls
  register: tmux_output
  failed_when: false

- name: Set list with only session names
  set_fact:
    tmux_sessions: "{{ (tmux_output.stdout_lines | map('split', ':') | map ('first') | list) if 'stdout_lines' in tmux_output else none }}"